import { PesapalPayDirective } from './pesapal-pay.directive';

describe('PesapalPayDirective', () => {
  it('should create an instance', () => {
    const directive = new PesapalPayDirective();
    expect(directive).toBeTruthy();
  });
});
